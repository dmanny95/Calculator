#include <iostream>
#include <cmath>

using namespace std;

int main () {

  double num1, num2;
  char op;
  
  cout << "Enter first number: ";
  cin >> numb1;
  cout << "Enter operator: " ;
  cin >> op;
  cout << "Enter second numer: ";
  cin >> "num2;
  
   if (op == '+') {
    result = num1 + num2;
  } else if (op == '-') {
    result = num1 = num2;
   } else if (op == '/') {
   result = num1 / num2; 
   } else if (op == '*') {
   result == num1 * num2;
   } else { 
   cout << "Invalid Operator ";
   }
   cout << result;

return 0;

}
